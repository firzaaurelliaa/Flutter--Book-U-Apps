import 'package:flutter/material.dart';

class KasirHome extends StatelessWidget {
  const KasirHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Home Kasir'),
        elevation: 0,
      ),
    );
  }
}
